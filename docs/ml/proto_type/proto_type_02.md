# コンポーネントの名前
- proto_type_02:
    - プレー中検出モデルの作成
    - 卓球プレイヤー or others の判定

## 概要
このコンポーネントは、卓球プレー映像からプレー中のシーンを自動的に検出し、切り抜く機能を提供します。YOLOv11-poseを用いて選手の骨格データを取得し、プレー中とプレー外の動作を判別します。

## 機能要件
- 動画ファイルを入力として受け取り、プレー中のシーンを抽出する。
- 学習させるデータについて
    - 初期モデルとしては多様性については考慮しない
    - 右対右のプレー映像，画角は固定，プレースタイルはドライブマンを想定する．

## 入力
- 動画ファイル（例: MP4, MOV形式）

## 出力
- 抽出されたプレー中のシーンを含む動画ファイル

## 処理フロー
1. 動画ファイルを受け取る
- Task1: プレイヤーorプレイヤー外の判定
    1. YOLOv11を用いて卓球台のバウンディングボックスの座標を獲得
        - 粗いフレーム数で獲得する(基本座標は同じであるため，カメラが移動した場合などを考慮する，かつ処理時間短縮のため)
        - 検出する卓球台は画面中央に近い卓球台とする
        - バウンディングボックスの座標情報のみを取得
    2. YOLOv11-poseを用いて動画内の全人間の骨格データを取得
        - 粗いフレーム数で獲得する
    3. 骨格データの運動量を計算してプレイヤー候補を絞り込む
        - 骨格データの運動量を計算（例えば，審判などは停止しているから除外したい）
        - tracking IDの継続性を活用してプレイヤー候補を追跡
        - 卓球台との近さなども考慮してプレイヤー候補を絞り込む
        - 仮にプレイヤーとして判定されたとしても，そのプレイヤーとしてのトラッキング時間が短いなら削除する．
    4. 出力の想定
      - 全フレームとプレイヤーとして判定された人間の骨格データ
- Task2: プレイ中のシーン抽出
    - 入力として受け取るのはTask1の結果
    1. プレイヤーの動きが活発なフレームをプレー中と判定
    2. 連続するプレー中のフレームをまとめて抽出
    3. 判定モデルLSTMを用いて，プレー中シーンの精度向上を図る

### 簡略化フロー
- Step1: 卓球台検出 (0.10fps)
  - YOLOによる物体検出でバウンディングボックスを取得
  - 画面中央に最も近い卓球台を選択
  - バウンディングボックスの座標情報を保存

- Step2: プレイヤー候補検出 (1fps)
  - YOLOv11-poseで映像内すべての人物の骨格データを検出
  - 検出された全ての人物候補を取得

Step2.5: プレイヤー確定
  - Step2で得られた複数の候補から最終的に1 or 2人を選定
  - 選定基準:
    1. 卓球台のバウンディングボックスに近い：
      - 仮に近くに来ないならそれは検出外とする．
    1. 総運動量が多い
      - 審判の除外

  → tracking IDと時系列情報から一時的な検出の途切れを補完

Step3: Dense骨格データ取得 (30fps)
  - Step2.5で確定したプレイヤーの出現区間
  - ここでも track_buffer を活用して途切れ対策

Step4: LSTM判定 + 補間処理
  - プレー中判定
  - 骨格データが途切れた区間は時系列補間
    例: 前後5フレームの平均で欠損フレームを補完

## コンポーネント定義（想定）
    TableDetector (卓球台検出コンポーネント)
    ├─ 責務: 卓球台の検出とバウンディングボックス取得
    ├─ 入力: フレーム画像
    └─ 出力: TableInfo (bbox, confidence, frame_idx)

    PlayerDetector (プレイヤー検出コンポーネント)
    ├─ 責務: プレイヤー候補の検出・tracking・確定
    ├─ 入力: フレーム画像
    └─ 出力: プレイヤーのtracking ID一覧と骨格データ

## テストケース
- 正常系: 有効な動画ファイルを入力として与え、プレー中のシーンが正しく抽出されることを確認する。
- 異常系: 存在しない動画ファイルを入力として与え、適切なエラーメッセージが表示されることを確認する。

## レビュー指摘事項
- []

## その他
- [] 検出安定性を向上させる技術的対策
  - マルチスケール検出
    - config
      - "imgsz": 1280,  # デフォルト640より大きく
      - "conf": 0.3,    # 信頼度閾値を下げて検出漏れを防ぐ
      
  - Tracking アルゴリズムの強化
    - YOLOv11のデフォルトtrackingではなく、より堅牢なものを使用
    - tracker_config
      - "tracker_type": "bytetrack",  # or "botsort"
      - "track_high_thresh": 0.3,
      - "track_low_thresh": 0.1,
      - "new_track_thresh": 0.4,
      - "track_buffer": 30,  # ←重要: 検出が途切れても30フレーム追跡継続
      - "match_thresh": 0.8,